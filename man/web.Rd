% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sources.R
\name{web}
\alias{web}
\title{Declare a web documentation source}
\usage{
web(name, root_url, pattern = NULL)
}
\arguments{
\item{name}{Short source label, recorded on every chunk and usable as the
\code{source} filter in \code{\link[=search_docs]{search_docs()}}. Must be unique within a \code{\link[=sources]{sources()}}
spec.}

\item{root_url}{The site to crawl.}

\item{pattern}{Optional regular expression; only links matching it are
ingested. Use it to stay within one book or section and skip nav/asset
links, e.g. \code{"^https://docs\\\\.example\\\\.com/[^#]+\\\\.html$"}.}
}
\value{
A \code{ragdoc_source} object, to be passed to \code{\link[=sources]{sources()}}.
}
\description{
Describes one site to crawl and ingest into a store. \code{\link[=build_store]{build_store()}} visits
\code{root_url}, follows the links found there (optionally filtered by \code{pattern}),
converts each page to markdown, chunks it, and records \code{name} on every chunk
so results can be cited and filtered by source.
}
\examples{
web("handbook", "https://docs.example.com/", pattern = "\\\\.html$")
}
\seealso{
\code{\link[=sources]{sources()}}, \code{\link[=build_store]{build_store()}}
}

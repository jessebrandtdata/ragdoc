% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{search_docs}
\alias{search_docs}
\title{Search the documentation store}
\usage{
search_docs(store, query, n = 8, source = NULL)
}
\arguments{
\item{store}{A connected ragnar store (see \code{\link[=connect_store]{connect_store()}}).}

\item{query}{Natural-language search query.}

\item{n}{Number of chunks to return. Defaults to 8.}

\item{source}{Optional source name to restrict to (one of the names you gave
to \code{\link[=web]{web()}}).}
}
\value{
A single formatted string of cited passages, \code{"no matches"}, or a
\code{"retrieval failed: ..."} message.
}
\description{
The core retrieval entrypoint, and what \code{\link[=serve_mcp]{serve_mcp()}} exposes to a coding
agent. Retrieves the most relevant passages for \code{query} from the store,
optionally restricted to one \code{source}, and formats them with source-and-URL
citations.
}
\details{
When a \code{source} filter is given, retrieval over-fetches (\code{n * 4}) before
filtering so the cap still yields up to \code{n} in-source chunks. Retrieval is
network- and API-backed; failures are caught and returned as a short message
rather than raised, so a server built on this never crashes.
}
\examples{
\dontrun{
store <- connect_store("handbook.duckdb")
cat(search_docs(store, "how do I rotate the signing keys", n = 5))
}
}
\seealso{
\code{\link[=connect_store]{connect_store()}}, \code{\link[=serve_mcp]{serve_mcp()}}
}

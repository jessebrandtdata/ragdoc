% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{load_sources}
\alias{load_sources}
\title{Load documentation sources from a YAML file}
\usage{
load_sources(path)
}
\arguments{
\item{path}{Path to the YAML file.}
}
\value{
A \code{ragdoc_sources} object (see \code{\link[=sources]{sources()}}), ready for \code{\link[=build_store]{build_store()}}.
}
\description{
Reads a \code{sources.yml} file into a \code{\link[=sources]{sources()}} spec, so a corpus can be kept
declaratively in one config file instead of in R code. Each top-level YAML
entry becomes one source: an entry with a \code{root_url} is a \code{\link[=web]{web()}} source, and
an entry with a \code{path} is a \code{\link[=local_dir]{local_dir()}} source.
}
\details{
The recognized fields mirror the constructor arguments:

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{- name: handbook              # web source
  root_url: https://docs.example.com/
  crawl_pattern: "\\\\.html$"   # optional, -> web(pattern=)
- name: notes                 # local source
  path: ~/project/docs
  file_pattern: "\\\\.md$"      # optional, -> local_dir(pattern=)
  recursive: false            # optional, -> local_dir(recursive=)
}\if{html}{\out{</div>}}

Requires the \pkg{yaml} package.
}
\examples{
\dontrun{
src <- load_sources("sources.yml")
build_store(src, "docs.duckdb")
}
}
\seealso{
\code{\link[=sources]{sources()}}, \code{\link[=web]{web()}}, \code{\link[=local_dir]{local_dir()}}, \code{\link[=build_store]{build_store()}}
}

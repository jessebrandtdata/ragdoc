% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{connect_store}
\alias{connect_store}
\title{Connect to a store with full-text search enabled}
\usage{
connect_store(path)
}
\arguments{
\item{path}{Path to the DuckDB store created by \code{\link[=build_store]{build_store()}}.}
}
\value{
A connected, read-only ragnar store object.
}
\description{
Opens the DuckDB store read-only and best-effort loads DuckDB's \code{fts}
extension on the connection. ragnar's hybrid retrieval relies on a
\code{match_bm25} macro that lives in \code{fts}, and DuckDB loads extensions
\emph{per connection} -- so a fresh process can hit
\verb{Catalog Error: Scalar Function with name match_bm25 does not exist} even
when the BM25 index is intact on disk. Loading \code{fts} here keeps hybrid
(vector + keyword) search working; keyword search matters for exact-token
queries an agent makes, like an error code or a function name. \code{LOAD} is
offline once the extension is installed, and failures are swallowed because
the query path (\code{\link[=search_docs]{search_docs()}}) still guards retrieval.
}
\examples{
\dontrun{
store <- connect_store("handbook.duckdb")
}
}
\seealso{
\code{\link[=build_store]{build_store()}}, \code{\link[=search_docs]{search_docs()}}
}
